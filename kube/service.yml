---
apiVersion: v1
kind: Service
metadata:
  name: kk6gpv-web-service
  annotations:
    service.beta.kubernetes.io/do-loadbalancer-enable-proxy-protocol: "true"
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 4000
  selector:
    name: kk6gpv-web-app

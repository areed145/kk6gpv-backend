---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kk6gpv-mqtt
spec:
  replicas: 1
  selector:
    matchLabels:
      name: kk6gpv-mqtt
  template:
    metadata:
      labels:
        name: kk6gpv-mqtt
    spec:
      containers:
        - name: application
          image: areed145/kk6gpv-mqtt:$COMMIT_SHA1
          imagePullPolicy: Always
          ports:
            - containerPort: 4000
